import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App smoke test', (WidgetTester tester) async {
    // Basic smoke test — actual widget tests require mocked providers
    expect(true, isTrue);
  });
}
